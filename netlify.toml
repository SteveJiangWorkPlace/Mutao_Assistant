[build]
  publish = "dist"
  command = "npm run build"
  installCommand = "npm install"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "18"
  NODE_ENV = "production"
  PYTHON_VERSION = ""  # 禁用Python安装，避免Netlify检测Python配置文件